// import { useMutation } from "@apollo/client"
// import { INSERTUSER } from "../graphql/mutation"

export const Users = () => {
  //   const [insertUser, { data, loading, error }] = useMutation(INSERTUSER)
  //   if (loading) return "Submitting..."
  //   if (error) return `Submission error! ${error.message}`
  return (
    <div>
      Users
      {/* <button onClick={insertUser({ variables: { name: "Hello" } })}>
        Add User
      </button> */}
    </div>
  )
}
